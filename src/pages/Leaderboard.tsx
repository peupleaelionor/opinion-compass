import { Medal, Trophy } from "lucide-react";
import { topAnalysts } from "@/data/zawios";
export default function Leaderboard() { return <div className="safe-shell mx-auto max-w-5xl pb-24 pt-8 md:pb-12"><span className="z-pill"><Trophy className="mr-1 h-3.5 w-3.5" />Classement public</span><h1 className="mt-3 font-display text-4xl font-bold md:text-5xl">Leaderboard</h1><div className="mt-6 grid gap-3 md:grid-cols-3">{topAnalysts.slice(0,3).map((a, i) => <div key={a.username} className="z-glass p-4"><Medal className="mb-5 h-6 w-6 text-primary" /><p className="z-micro-label">rang {i + 1}</p><p className="mt-1 font-display text-xl font-bold">@{a.username}</p><p className="mt-2 text-sm text-muted-foreground">{a.badge} · {a.accuracy}% précision</p><p className="mt-4 font-display text-3xl font-bold text-primary">{a.score}</p></div>)}</div><div className="mt-4 grid gap-3">{topAnalysts.concat(topAnalysts).map((a, i) => <div key={`${a.username}-${i}`} className="z-card flex min-h-20 items-center justify-between gap-4 p-4 transition-all hover:-translate-y-0.5 hover:shadow-soft"><div className="flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-lg bg-brand text-primary-foreground font-bold">{i + 1}</div><div><p className="font-bold">@{a.username}</p><p className="text-sm text-muted-foreground">{a.badge} · streak {a.streak} jours</p></div></div><div className="text-right"><p className="font-display text-2xl font-bold text-primary">{a.score}</p><p className="text-xs font-semibold text-muted-foreground">{a.accuracy}% précision</p></div></div>)}</div></div>; }

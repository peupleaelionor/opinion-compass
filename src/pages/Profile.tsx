import { useParams } from "react-router-dom";
import { Camera, ShieldCheck } from "lucide-react";
import { Button } from "@/components/ui/button";
export default function Profile() { const { username = "claire_signal" } = useParams(); return <div className="safe-shell mx-auto max-w-4xl pb-24 pt-8 md:pb-12"><div className="z-card overflow-hidden"><div className="relative h-40 bg-hero"><div className="absolute inset-x-8 bottom-8 h-px bg-primary/25" /><div className="absolute right-8 top-8 h-20 w-40 rounded-lg border border-line bg-glass shadow-card" /></div><div className="p-5"><div className="-mt-16 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><div className="flex h-24 w-24 items-center justify-center rounded-2xl border-4 border-card bg-brand text-3xl font-bold text-primary-foreground shadow-perspective">{username[0]?.toUpperCase()}</div><h1 className="mt-3 font-display text-4xl font-bold">@{username}</h1><p className="mt-2 max-w-xl text-muted-foreground">Analyste public. Vote avec nuance, privilégie les horizons vérifiables et les commentaires courts.</p></div><Button variant="outline"><Camera /> Upload photo</Button></div><div className="mt-6 grid grid-cols-2 gap-3 md:grid-cols-4">{[["Votes", "1 248"], ["Précision", "74%"], ["Streak", "18j"], ["Score", "982"]].map(([k,v]) => <div key={k} className="rounded-lg bg-surface p-4"><p className="text-xs font-bold text-muted-foreground">{k}</p><p className="font-display text-2xl font-bold">{v}</p></div>)}</div><div className="mt-5 flex flex-wrap gap-2"><span className="z-pill"><ShieldCheck className="mr-1 h-3.5 w-3.5" />Calibrée</span><span className="z-pill">Tech</span><span className="z-pill">Long terme</span></div></div></div></div>; }
